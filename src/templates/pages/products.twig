{% extends "master.twig" %}

{% block content %}
  {% include "elements/navbar.twig" with {currentPath: '/'} %}

  <div class="container-fluid">
  {% include "elements/filter.twig" with {type: 'Product'} %}
    <div class="row justify-content-end">
      <div class="p-0 pt-2 col-sm-6 col-md-7 col-lg-8 col-xl-9">
        <div class="container-fluid">
          <div class="row">
          {% for product in products %}
            {% include "elements/product.twig" %}
          {% endfor %}
          </div>
        </div>
      </div>
    </div>
  </div>

{% endblock %}