{% extends "master.twig" %}

{% block content %}
  {% include "elements/navbar.twig" with {currentPath: '/'} %}

  <div class="container-fluid">
  {% include "elements/filter.twig" %}
    <div class="row justify-content-end">
      <div class="col-sm-6 col-md-7 col-lg-8 col-xl-9">
        <div class="container-fluid">
          <div class="row">
            
            {% set product %}
              {% include "elements/product.twig" %}
            {% endset %}

            {{product}}
            {{product}}
            {{product}}
            {{product}}
            {{product}}
            {{product}}
            {{product}}
            {{product}}
            {{product}}
          </div>
        </div>
      </div>
    </div>
  </div>

{% endblock %}